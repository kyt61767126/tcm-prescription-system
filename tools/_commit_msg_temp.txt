feat(naming,upgrade): 统一命名 + Electron 升级 + capacitor 依赖清理

任务1: db-bendi 月度统计报表
- 经验证 db-bendi 已完整具备月度统计报表功能（根目录和 android assets 都是 8 处匹配）
- 与 db-dingzhi 完全一致，无需修改

任务2: 统一命名为 惠康中医-版本
- 4 个 config.json: productName 改为 惠康中医-本地/定制/个人/云端
- 4 个 android assets config.json: 同步修改
- 5 个 package.json: build.productName + description 改为带连字符
- 4 个 build-app.bat: PRODUCT_NAME fallback 改为中文带连字符
- 4 个 installer.nsh: NSIS 安装目录路径改为带连字符
- 4 个 main.js/electron/main.js: productName 默认值改为带连字符
- 4 个 strings.xml: app_name + title_activity_main 改为带连字符
- 1 个 capacitor.config.json: appName 改为带连字符
- 8 个 index.html: title + alert + version 字符串改为带连字符（共 22 处）

任务3: Electron 保守升级 28/31 → 35+
- 5 个 package.json: electron 版本改为 ^35.0.0
- 保守升级策略：仅修改版本号，需用户手动执行 npm install 并测试
- 升级后需重新打包桌面程序验证视频录制等功能

任务4: 移除未使用的 @capacitor/* 依赖
- package.json: 移除 @capacitor/app 和 @capacitor/network
- capacitor.settings.gradle: 移除 capacitor-app 和 capacitor-network include
- capacitor.build.gradle: 移除对应 implementation project
- 验证：业务代码中未使用 Capacitor.Plugins.App 和 Capacitor.Plugins.Network
- 保留：@capacitor-community/sqlite（数据库）和 @capacitor/preferences（偏好设置）

后续操作（用户需手动执行）：
1. 在项目根目录执行 npm install 更新依赖
2. 在 cloud_project/cloud_app 执行 npx capacitor update 同步插件配置
3. 测试桌面程序打包（验证 Electron 35 兼容性）
4. 测试云端 APP 打包（验证 capacitor 依赖移除无影响）
