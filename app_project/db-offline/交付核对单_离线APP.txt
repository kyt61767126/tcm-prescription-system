╔══════════════════════════════════════════════════════════════════╗
║                 交付核对单（DELIVERY CHECKLIST）                   ║
╚══════════════════════════════════════════════════════════════════╝

端类型      : 离线APP（offline_app）
⚠ 未找到 build-meta.json（三元组缺失，手工核对版本）

── 产物清单 ──────────────────────────────────────────────────────
  惠康中医-本地.apk
    大小 2.7 MB  |  修改 2026-08-21 15:47:51
    sha256 7df2234e725ec8cc5d2ca04d2164ae0eaf8c7c9a3af9681dd2f82b41138a0ec1

── 运行时冒烟（本次实时复算）─────────────────────────────────────
  134/134 ✓（7 表面 + login 旁路）

── 本次构建已通过的关卡（APP 严格打包流程）─────────────────────
  ① 预编译（assets 同步 shared 权威源标记块/文件）
  ② 非严格打包 + 严格模式打包（Java 层混淆 + 签名校验）
  ③ APK 签名哈希硬校验
  ④ 源级冒烟 134 用例（本报告实时复算，见上）

── 安装自检三步（装完 30 秒核对，杜绝假包/旧进程误判）──────────────
  ① 装新版前：任务管理器搜"惠康"杀光旧进程（旧进程锁文件+残留窗口会造成
     "装了新版还是旧行为"假象）；
  ② 装完登录页底部核对三元组：V? | Build ? | Arch ?
     对不上 = 装到缓存旧包，删掉重下；
  ③ 云端桌面版额外一步：悬停【用户管理】按钮看 tooltip 水印（Arch）再次确认。

── 各端生效方式速查 ───────────────────────────────────────────────
  云端网页版 / 云端APP   : 推 GitHub 自动部署，在线即时生效，无需重打 APK
  云端桌面版             : 重新 build.bat 打包 exe，重装后重新登录一次
  离线桌面版             : 重新 build.bat 打包 exe
  离线APP                : 重打 惠康中医-本地.apk 并重装
  纯后端 functions       : 推 GitHub 自动部署即生效

── 排障速查 ───────────────────────────────────────────────────────
  症状"装了新版还报旧问题"：
    → 先看三元组。对不上=假包或旧进程（Get-Process *惠康* 杀光重装），
      对得上才继续查代码（此时必有新代码 alert 兜底信息）。
  症状"登录窗口读不到账号/密码"：
    → 铁闸8b 已保证 login.js 委托 UserStore，先核对装的版本含本报告版本号。

报告生成时间 : 2026-08-21 16:54:14
生成器       : tools/delivery-report.cjs（P4 自动化，随构建产出）