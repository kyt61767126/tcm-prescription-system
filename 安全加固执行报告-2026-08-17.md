# 惠康中医诊所管理系统 · 安全加固（防破解）执行报告

- **日期**：2026-08-17
- **范围**：打包上传 APK / 桌面程序防破解加固，覆盖 Java、NDK、JS、界面 C 四层
- **总原则（硬红线）**：宁可漏检不可误报；不允许导致正常用户闪退；安全加固不得破坏打包流程。
- **涉及版本**：云端统一包 / 本地统一包；源码于 GitHub 自动推送后 Cloudflare Pages 自部署网页版。

---

## 一、执行总览

| 编号 | 加固项 | 层级 | 状态 | 验证结果 | 落地 commit |
|------|--------|------|------|----------|-------------|
| P0-① | 恢复 stringArray 混淆（base64） | JS | ✅ | v5.4.7 三种编码实测 base64 往返正确 | 早于本次清单 |
| P0-③ | 桌面 exe 签名自校验 | Electron | ✅ | 非阻塞记录式，预留签名位 | 早于本次清单 |
| P0-NDK | APK 签名校验下沉 NDK 原生层 | C/C++ | ✅ | 两端 .so 打入，双 ABI | `43f2e1a3` |
| P1-3/P2-5 | Java 层深度 R8 混淆 | Java | ✅ | repackageclasses 生效，MainActivity keep | `022623bb` |
| P1-4 | JS 混淆增强 + CSP 三端加固 | JS/HTML | ✅ | new Function 语法全通过；CSP 三端同步 | `80164cbd` |

---

## 二、执行明细

### 2.1 P0-①：恢复 stringArray 混淆（JS，base64）
- **目标**：恢复字符串数组化混淆，强化反静态分析。
- **做法**：`javascript-obfuscator v5.4.7` 下 `stringArrayEncoding: ['base64']`，`stringArrayThreshold: 0.5`。
- **决策依据**：实测 `none/base64/rc4` 三种编码在 Node(V8，与 Electron/WebView 同引擎)下对中文+关键字符串均往返正确；历史 `charAt` bug 为旧版缺陷、v5.4.7 已修复；选 base64 兼顾强度与稳定（无 rc4 历史运行时失败包袱）。
- **范围**：`permission/debug-logger/print-utils/medicine-dict/db-adapter/performance-utils/prescription-core/patient-archive/security-guard.js`。
- **红线保留**：`auth-core.js / login.js` 承担登录/激活校验核心链路，保守保持明文（密钥另存 config.json/localStorage，核心校验另有 Java/NDK 双保险）。

### 2.2 P0-③：桌面 exe 签名自校验（Electron 主进程）
- **目标**：为后续接入代码签名做好准备。
- **做法**：`self-check.js` 在`云端桌面版(cloud_desktop/electron)`与`离线桌面版(desktop/electron)`中检测 exe 代码签名状态。
- **红线**：当前 exe 未做代码签名，采用**非阻塞、仅写日志**方式检测，预留签名位；绝不对未签名 exe 阻断，不导致正常用户闪退。

### 2.3 P0-NDK：APK 签名校验下沉 NDK 原生层
- **目标**：把 APK 签名校验中最易被逆向的关键逻辑（SHA-256 + 常量时间比对）从 Java 下沉到 `.so` 原生层。
- **做法**（离线/云端各一套，JNI 符号随包名变化各存一份）：
  - `securityguard.cpp`：纯 C++ 实现 SHA-256（FIPS 180-4），无第三方依赖；`constantTimeEqualsIgnoreCase` 防时序侧信道。
  - `CMakeLists.txt`：构建 `libsecurityguard.so`，`-fvisibility=hidden` 缩小可逆向面。
  - `NativeGuard.java`：JNI 桥，`System.loadLibrary` 失败仅 `Log.w`，isAvailable() 供调用方判定回退。
  - 离线 `com.benneng.pres`（LicenseManager）、云端 `com.tcm.prescription`（SecurityGuard）优先走 native，native 不可用回退原 Java 实现。
  - `build.gradle`：`ndkVersion=28.2.13676358` + `externalNativeBuild{cmake}`；`proguard-rules.pro` 保留 `NativeGuard` 与 native 方法防 R8 破坏 JNI。
- **算法正确性**：`tools/verify-sha256.mjs` 将 C++ SHA-256 逐行等价翻译为 JS 与 Node crypto 比对，覆盖空串/`abc`/长度边界(56/64)全通过。

### 2.4 P1-3/P2-5：Java 层深度 R8 混淆（类重打包 + 多轮优化）
- **目标**：在既有 `-allowaccessmodification/-mergeinterfacesaggressively/-overloadaggressively` 基础上再做深度混淆。
- **做法**：`proguard-rules.pro` 新增 `-repackageclasses 'b'`（全部未 keep 类重打包到单块混淆包，等效 dex 加固的混淆层）+ `-optimizationpasses 5`。
- **验证**：mapping 确认库类与主工程类（含 SecurityGuard）已重打包到 `b.*`；`MainActivity`/`NativeGuard`/`@CapacitorPlugin` 保留原包名 → 启动与 JNI 绑定安全。
- **关于"dex 二次加壳"的明确结论**：真加壳（360/爱加密等）会对 APK 重打包改签名，必然破坏启用中的 v2/v3 签名 + NDK 签名校验闭环 → 自我误报退出，违反红线。故以 R8 `repackageclasses` 作零签名风险的等效加固；真加壳需第三方在线壳平台并重验签名注入流程，需单独立项。

### 2.5 P1-4：JS 混淆增强（语义无损项）+ CSP 三端加固
- **JS 混淆增强**（`tools/obfuscate.js`，全部语义无损）：
  - 字符串数组深度变形：`stringArrayCallsTransform`、`Shuffle`、`Rotate`、`WrappersChainedCalls`（2 层）。
  - `numbersToExpressions: true`：数字字面量转算术表达式。
  - `debugProtection: true` + `debugProtectionInterval: 0`：反 DevTools，interval=0 仅挂载不周期暂停，正常用户无感。
  - **不启用**（红线分界）：控制流平坦化、死代码注入、splitStrings（避免破坏 IndexedDB/SQL/加密盐字符串）、全局重命名、auth-core/login.js 混淆。
  - **验证**：`tools/verify-obf.js` 对 security-guard/permission/db-adapter 混淆后 `new Function` 语法全通过 → **ALL PASS**。
- **CSP 加固**（经用户同意改界面文件；三端 5 个 index.html）：追加 `form-action 'self'; upgrade-insecure-requests; worker-src 'self' blob:`；
  - 保留已有 `default-src 'self' / script-src 'self' 'unsafe-inline' / style-src 'self' 'unsafe-inline' / img-src 'self' data: blob: / media-src / font-src / connect-src 'self' / object-src 'none' / base-uri 'self'`。
  - 说明：CSP meta 无法携带 `frame-ancestors`/`report-to`；如需头部级更强 CSP 需在部署层（Cloudflare Pages 响应头）实施。

---

## 三、验证结果汇总

| 验证项 | 结果 |
|--------|------|
| SHA-256 C++ 实现与 Node crypto 一致性 | ✅ 全用例通过 |
| 离线 APK 内含 NDK .so | ✅ `lib/{arm64-v8a,armeabi-v7a}/libsecurityguard.so` |
| 云端 APK 内含 NDK .so（含 libsqlcipher） | ✅ 双 ABI 均含 |
| R8 repackageclasses 生效 | ✅ 库类/主工程类 → `b.*`；MainActivity/NativeGuard keep |
| JS 语义无损混淆后语法 | ✅ `new Function` ALL PASS（3 代表模块） |
| 两端 strict 打包流程（预编译→严格打包→签名哈希→JS 混淆/还原→APK 签名哈希） | ✅ PASS |
| 混淆还原 .bak 残留检查 | ✅ 通过，开发环境已还原 |
| APK 签名校验（v2/v3 + NDK 原生） | ✅ 闭环完好，重打包会被拦截 |

---

## 四、各端生效方式

| 端 | 是否需重打 | 生效方式 |
|----|-----------|----------|
| 云端网页版 | 否（推送即生效） | `git push` 后 Cloudflare Pages 自动部署（含 CSP meta 与混淆 JS） |
| 云端桌面版 exe | **是** | 重新运行 `cloud_desktop` 的 `build.bat` 打包 exe |
| 离线桌面版 exe | **是** | 重新运行 `db-offline/desktop` 的 `build.bat` 打包 exe |
| 云端 APP (.apk) | **是** | 使用重打的 `public/downloads/惠康中医-云端.apk` 重新安装 |
| 离线 APP (.apk) | **是** | 使用重打的 `public/downloads/惠康中医-本地.apk` 重新安装 |

> 已更新 `public/downloads/*.apk` 与 `public/hash-manifest.json`。
> 注意：`app_project/db-offline/app/app/src/main/assets/public` 属 `.gitignore` 忽略（由构建从共享源同步），只提交 APK 即可。

---

## 五、上线前待办（重要）

1. **真机自测**：R8 深度混淆与 JS 深度变形的最终效果需在真机 Android WebView 中安装自测，确认**无闪退、功能正常**（登录/改密/处方/统计/备份/录像）后再正式下发。
2. **桌面 exe 自测**：重打 exe 后确认主进程签名自校验逻辑仅记录日志、不影响正常启动。
3. **网页回归**：CSP 增量后确认线上页面脚本/样式/媒体加载正常（尤其 `upgrade-insecure-requests` 对本地/回源资源的影响）。
4. （可选，需单独立项）真·dex 加壳若需实施，须走第三方在线壳平台并重验 v2/v3 签名 + NDK 自校验闭环。

---

*归档说明：本报告由 AI 根据本次安全加固执行过程与 memory（第二十九～三十二章）汇总生成，commit 关联 `43f2e1a3 / 022623bb / 80164cbd`。*