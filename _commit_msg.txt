feat: 状态栏"【云版】"改为动态"在线/离线"显示

根据 navigator.onLine 实时切换：
- 在线：绿色【在线】
- 离线：红色【离线】

实现：
1. HTML 中固定"【云版】"改为带 id 的 span#modeStatus
2. 新增 updateModeStatus() 函数更新显示
3. updateUserDisplay() 中调用 updateModeStatus()
4. DOMContentLoaded 时初始更新 + 绑定 online/offline 事件监听
