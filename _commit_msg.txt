fix: 在线/离线状态初始保守显示离线，避免误显示在线

问题：管理员未开通云端功能时，本地有网络但云端 API 不可达，
状态栏仍显示"在线"（因初始用 navigator.onLine 兜底）。

修复：updateModeStatus 严格依据 _cloudReachable 判断，
null（未检测）时保守显示"离线"，只有 cloudFetch 真正成功
才显示"在线"。这样未开通云端功能的账号登录后，处方 API
调用失败，状态正确显示"离线"。
